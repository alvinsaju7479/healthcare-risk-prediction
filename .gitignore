# Virtual env
.venv/
venv/

# Python cache
__pycache__/
*.pyc

# Data + models (do not commit)
data/raw/
data/processed/
models/

# Optional: keep reports tracked (recommended)
# (Do NOT ignore reports/figures)
